services:
  web:
    image: ghcr.io/ivanpashkulev/web-main:7162f823dc4b16f5aa648e5bfd8bb73c770447bd
    container_name: web-main
    restart: unless-stopped
    environment:
      - VITE_API_URL=/api
      - VITE_TURNSTILE_SITE_KEY=${TURNSTILE_SITE_KEY:?TURNSTILE_SITE_KEY is required}
    depends_on:
      - api

  api:
    image: ghcr.io/ivanpashkulev/api-main:e8ed91f8781002aef0a656393590d0209235a5f5
    container_name: api-main
    restart: unless-stopped
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:?OPENAI_API_KEY is required}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
      OPENAI_MAX_OUTPUT_TOKENS: ${OPENAI_MAX_OUTPUT_TOKENS:?OPENAI_MAX_OUTPUT_TOKENS is required}
      CHAT_RATE_LIMIT_PER_DAY: ${CHAT_RATE_LIMIT_PER_DAY:?CHAT_RATE_LIMIT_PER_DAY is required}
      CHAT_MAX_MESSAGE_CHARACTERS: ${CHAT_MAX_MESSAGE_CHARACTERS:?CHAT_MAX_MESSAGE_CHARACTERS is required}
      CHAT_MAX_HISTORY_CHARACTERS: ${CHAT_MAX_HISTORY_CHARACTERS:?CHAT_MAX_HISTORY_CHARACTERS is required}
      REDIS_URL: redis://redis:6379/0
      TURNSTILE_SECRET_KEY: ${TURNSTILE_SECRET_KEY:?TURNSTILE_SECRET_KEY is required}
      TURNSTILE_EXPECTED_HOSTNAME: ivanpashkulev.com
    depends_on:
      redis:
        condition: service_healthy

  redis:
    image: redis:8.10.1-alpine
    container_name: redis
    command: redis-server --save "" --appendonly no
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3

  dj:
    image: ghcr.io/ivanpashkulev/dj:93fbb061810f2c9299264d03b5e2c93129ca214c
    container_name: dj
    restart: unless-stopped

  nginx:
    image: nginx:1.30.4-alpine
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - type: bind
        source: /etc/nginx/ssl
        target: /etc/nginx/ssl
        read_only: true
        bind:
          create_host_path: false
    depends_on:
      - web
      - api
      - dj
    restart: unless-stopped
